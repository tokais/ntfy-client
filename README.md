This projects contains a client side implementation to send message via https://ntfy.sh/ to my phone.
It's a template i use for long running algorithms, eg. neural network training.
To use it, set the SUBSCRIPTION variable and optionaly AUTH_TOKEN and you're good.
